#include <iostream>
#include <string>
using namespace std;
int main()
{
	string s;
	cin>>s;
	int count=0, countx=0;
	for(int i=0; i<s.length(); i++)
	{
		if(s[i]=='z')
		count++;
		else
		countx++;
		}
		count=count*2;
		if(count==countx)
		
		cout<<"Yes";
		else
		cout<<"No";
	return 0;
}
